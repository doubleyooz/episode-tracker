import tw from "@/src/constants/tailwind";
import { SafeAreaView } from "react-native";

export default function ViewAnimeScreen() {
  return (
    <SafeAreaView style={[tw`flex flex-1 flex-column m-0 p-0`]}></SafeAreaView>
  );
}
